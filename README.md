# Exercise: Opening a browser inside a WSL2 docker container

This exercise shows how a browser can be opened from a docker container
running inside a WSL distribution.

## Usage:

```sh
make run
```
